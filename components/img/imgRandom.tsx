import Image from 'next/image';

export const imgRandom = (props:any) => {

    const imgs = ['Miss.svg','Miss2.svg'];
    const imgNumer = Math.floor( Math.random() * imgs.length);

    return(
        <>
        
        </>
    )
}